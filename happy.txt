public boolean isHappyNumber(int n) {
  int max = 1000000;
  while (n != 1 && n <= max) {
    int sum = 0;
    while (n > 0) {
      int digit = n % 10;
      sum += digit * digit;
      n /= 10;
    }
    n = sum;
  }
  return n == 1;
}